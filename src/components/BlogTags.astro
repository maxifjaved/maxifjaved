---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;

if (!tags || tags.length === 0) {
  return null;
}
---

<p class="max-w-3xl font-mono text-xs leading-relaxed text-ink-3">
  <span class="uppercase tracking-wide">Filed under</span>
  {" "}{tags.join(" · ")}
</p>
