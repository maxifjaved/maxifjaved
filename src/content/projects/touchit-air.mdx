---
title: "TouchIT Air - Android AirPlay Receiver"
description: "Built the Android AirPlay receiver behind TouchIT Air, turning TouchIT interactive LED displays into native AirPlay targets. Integrates an open-source AirPlay/RAOP C core via the NDK, adds hardware-accelerated video and audio playback, and wraps it in a license-activated, self-updating commercial product for the TouchIT App Store."

category: "interactive-displays"
featured: false
status: "completed"

startDate: "2019-01"
endDate: "2020-12"
duration: "2019–2020 active development; later maintenance"

role: "Lead Developer (primary author)"
teamSize: "Primary developer — 36 of 38 commits; minor contributions from a colleague (design not handled)"

technologies: ["Java (Android)", "C/C++ AirPlay/RAOP core (NDK + CMake/JNI)", "Apple mDNSResponder (Bonjour)", "FairPlay/playfair handshake", "curve25519/ed25519 pairing", "Android MediaCodec", "fdk-aac + AudioTrack", "Android Volley", "Node.js"]
tags: ["AirPlay", "Screen Mirroring", "Android Development", "Interactive Displays", "Wireless Display", "Apple Ecosystem"]

links:
  liveUrl: null
  companyUrl: "https://touchittechnologies.com/touchitair/"
  caseStudyUrl: null
  demoUrl: null

images:
  featuredImage:
    src: "/img/projects/touchit-air-hero.jpg"
    alt: "TouchIT Air iOS screen mirroring solution"
    width: 1200
    height: 630
  thumbnail:
    src: "/img/projects/touchit-air-thumb.jpg"
    alt: "TouchIT Air thumbnail"
    width: 400
    height: 300
  gallery: []

seo:
  title: "TouchIT Air - AirPlay Receiver | Muhammad Asif Javed"
  description: "Android AirPlay receiver for TouchIT interactive LED displays: NDK-integrated RAOP/FairPlay core, hardware H.264 decode, license activation, and OTA updates."
  keywords: ["AirPlay receiver", "Android NDK", "RAOP", "FairPlay", "MediaCodec", "TouchIT Technologies", "screen mirroring", "interactive displays"]

businessContext:
  client: "TouchIT Technologies"
  company: "TouchIT Technologies"
  industry: "Educational Technology / Interactive Displays"
  userBase: "Educators and presenters using TouchIT interactive LED displays"

keyAchievements:
  - "Integrated the open-source AirplayServer C core into a packaged Android app via the NDK and CMake, loading the native raopserver_android library through JNI and bridging its callbacks into Java"
  - "Built hardware-accelerated video rendering, decoding the incoming H.264 mirror stream with Android MediaCodec straight onto a Surface with scale-to-fit for large LED panels"
  - "Implemented audio playback, feeding fdk-aac-decoded PCM packets into an Android AudioTrack tuned for stable playback over classroom Wi-Fi"
  - "Enforced single-connection, uninterrupted-presentation mode so a second iOS device on the network can't hijack the display mid-presentation"
  - "Built a license-key activation flow gated behind a splash screen that validates the license against a backend before the receiver starts"
  - "Added over-the-air self-update so deployed LED units fetch and install the latest TouchITAir.apk without manual reinstalls"
  - "Stood up the companion Node.js backend serving version-check, license-verification, and APK-distribution endpoints over Volley"

metrics:
  - label: "Commit Authorship"
    value: "36 of 38 commits"
    description: "Primary author of the TouchIT product layer, with minor colleague contributions"
  - label: "Extra Hardware Required"
    value: "Zero"
    description: "Software-only AirPlay receiver — no Apple TV or dongle needed"
  - label: "Video Decode"
    value: "Hardware H.264"
    description: "MediaCodec hardware-accelerated decode straight to Surface"

keyFeatures:
  - "Android AirPlay receiver - turns a TouchIT LED into an AirPlay target for iPhone, iPad, and Mac mirroring"
  - "No extra hardware - software-only, no Apple TV or AirPlay dongle required"
  - "Bonjour/mDNS discovery - shows up natively in Apple's Screen Mirroring list"
  - "Hardware H.264 video - MediaCodec decode to Surface with scale-to-fit for large panels"
  - "Synchronized audio - fdk-aac to PCM playback via AudioTrack"
  - "Single-connection lock - uninterrupted presenter control on shared networks"
  - "License-key activation - registration and validation against a backend"
  - "Over-the-air updates - in-app version check and APK self-update"
  - "Companion licensing backend - Node.js version/license/APK distribution service"
  - "TouchIT App Store distribution - packaged for deployment across LED installations"

impactResults:
  - "Eliminated dedicated AirPlay hardware, with a software-only receiver on the LED itself replacing an Apple TV or AirPlay dongle per room"
  - "Delivered a native Apple mirroring workflow that appears in the standard iOS/macOS Screen Mirroring list via Bonjour, with no app required on the presenter's device"
  - "Kept presentations interruption-free through single-connection locking that keeps one presenter in control on shared classroom and boardroom networks"
  - "Enabled maintainable fleet deployment, with license activation plus in-app OTA updates letting TouchIT distribute and keep the receiver current across LED installations"
---

## Overview

TouchIT Air turns an Android-powered TouchIT interactive LED display into an AirPlay receiver, so teachers and presenters can wirelessly mirror an iPhone, iPad, or Mac screen straight to the board — no Apple TV, no AirPlay dongle, no extra hardware. The app advertises itself on the local network over Bonjour/mDNS, completes Apple's AirPlay pairing and FairPlay handshake, then receives the mirrored video and audio stream and renders it full-screen on the LED. It was built as a packaged, license-activated Android product for the TouchIT App Store.

The AirPlay protocol core is the open-source **AirplayServer** project (originally by *dsafa22*; the bundled C library handles RAOP, FairPlay, pairing, mDNS, and fdk-aac audio decoding under its respective BSD/MIT/LGPL/GPL licenses). The work documented here is the **TouchIT product layer on top of it**: integrating that C core into a real Android app via the NDK and JNI, wiring up hardware video/audio playback, and adding everything needed to ship it commercially — license-key activation, an over-the-air update flow, single-connection presentation locking, and a supporting backend.

## Technical Implementation

The AirPlay receiver logic lives in a C library compiled into the app with CMake (`armeabi-v7a`, `-std=c++11`). It implements mDNS service advertisement (Apple's mDNSResponder), the AirPlay pairing/handshake (curve25519 + ed25519), the FairPlay/playfair exchange, the RAOP/RTP streaming endpoints, and fdk-aac audio decoding. Java talks to it through a thin JNI bridge (`RaopServer` loading `raopserver_android`), which hands decoded NAL video packets and PCM audio packets back up to the app.

On the Android side, `VideoPlayer` wraps `MediaCodec` as an H.264 decoder configured against the display `Surface` with `VIDEO_SCALING_MODE_SCALE_TO_FIT`, while `AudioPlayer` wraps `AudioTrack` in streaming mode and writes buffered PCM packets; a small model layer (`NALPacket`, `PCMPacket`) carries frames between the native callbacks and the players. On the product side, `KeyActivity` captures the license key and email, and `SplashActivity` uses Volley to hit the licensing backend for version checks, license verification, and `TouchITAir.apk` downloads, gating activation and driving OTA updates. The backend is a Node.js service on a self-managed Linux VPS exposing those version/license/APK endpoints.

## Impact & Results

TouchIT Air eliminated the need for dedicated AirPlay hardware: a software-only receiver running on the LED itself replaces an Apple TV or AirPlay dongle per room, and the display appears in the standard iOS/macOS Screen Mirroring list via Bonjour with no app required on the presenter's device. Single-connection locking keeps a presenter in control on shared classroom and boardroom networks, and license activation plus in-app OTA updates let TouchIT distribute and keep the receiver current across LED installations through its App Store.

The AirPlay/RAOP core is built on the open-source **AirplayServer** project (originally by *dsafa22*), which bundles third-party components — crypto, curve25519, ed25519, fdk-aac, playfair, plist, mDNSResponder, http-parser — under New BSD, MIT, Apache 2.0, LGPLv2.1+, and GPL licenses. The contribution documented here is the TouchIT-specific product layer on top of that foundation: NDK/JNI integration into a shippable Android app, MediaCodec/AudioTrack playback, single-connection locking, license activation, OTA updating, and the companion backend.
